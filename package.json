{
  "name": "week10dom",
  "version": "1.0.0",
  "description": "1. Using HTML, Bootstrap, and JavaScript create a single page website that contains the following:\r     a. A Bootstrap styled table representing your choice of data.\r     b. A Bootstrap styled form that allows a user to add a new row to the table when submitted.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^4.4.1",
    "jquery": "^3.6.0"
  }
}
